import React, { FunctionComponent } from 'react';

import ResetPassword from 'src/components/AuthLayout/ResetPassword';

const ResetPasswordPage: FunctionComponent<{}> = () => {
  return(
    <ResetPassword />
  )
};

export default ResetPasswordPage;
